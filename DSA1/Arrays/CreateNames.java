package Arrays;

public class CreateNames {
    public static void main(String args[]) {
        String names[] = {"ravi", "raj", "rask", "mahesh", "villen"};
        int n = names.length;
        for (int i = 0; i < n; i++) {
            System.out.print(names[i] + " * ");
        }
    }
}